# 交付：《寄海文库用户使用手册》PDF + README 重写

本轮交付用户提出的两件事：

1. **按项目真实代码梳理功能，重写 `README.md`**；
2. **一份带真实界面截图的《用户使用手册》PDF** —— 截图取自线上部署 `https://wiki.jihaihotpot.com/`。

> 上一版 `overview.md`（文库工作台 + CAD 字号修复）已随提交进入 git 历史，
> 用 `git log -p -- overview.md` 回看。

---

## 一、交付物

| 交付物 | 路径 | 规格 |
| --- | --- | --- |
| 使用手册 PDF | `docs/寄海文库用户使用手册.pdf` | **48 页 / 6.6 MB / A4**，49 张界面截图 |
| 手册源文件 | `docs/manual/寄海文库用户使用手册.md` | 17 章，约 1000 行 |
| 截图资源 | `docs/manual/img/` | 49 张 1600×1000 全分辨率 PNG |
| 排版样式 | `docs/manual/.manual-style.css` | A4 排版 + 满页封面 |
| 构建脚本 | `tools/build/build-manual-pdf.sh` | 一条命令重出 PDF |
| 重写的 README | `README.md` | 516 行，十节 |

重出 PDF：`bash tools/build/build-manual-pdf.sh`

---

## 二、README 重写要点

逐项对照代码核实后，**修正了原文档与实现不符的陈述**，其中影响最大的一条：

- ❌ 原文「首个注册的账号自动成为管理员」 → ✅ 实际**新注册账号一律 `role=member`**，管理员只能由初始化脚本播种或后台提权。
  （按原文操作会得到一个谁都进不去后台的系统。）

其余补充内容：环境与配置（SQLite/MySQL、中文字体、DWG 转换器）、权限模型、14 种 `doc_type` 全矩阵
（含导入/导出能力）、前端静态资源全自托管与三层按需加载、统一错误码表与路由分组、`exportx`/`imgconv` 结构、
回归套件用法、已知限制。

---

## 三、手册结构

```
封面 → 写在前面 → 目录
1 系统概览   2 登录与账号   3 界面总览   4 知识库（书架）
5 目录树与文档管理          6 十一种内容类型（逐类配图 + 导入/导出能力）
7 编辑、自动保存与历史版本   8 阅读体验   9 导入   10 导出
11 搜索     12 分享         13 协作与团队   14 回收站
15 管理员功能   16 效率技巧速查   17 常见问题
```

正文中所有截图均来自真实运行系统；管理员章节的系统配置截图已对 JWT 密钥与数据库连接串做遮罩。

---

## 四、截图采集方式（含一处例外，需知悉）

- **线上部署采集（48 张）**：用 `admin/Jihai2026` 登录，在线上播种了一个演示知识库
  「【演示】产品资料库（手册截图）」，覆盖全部 14 种文档类型 + 多级目录 + 团队 + 版本快照 + 分享链接。
- **本地 SQLite 实例补拍（1 张）**：**第 11 章「搜索结果页」是唯一例外**。
  原因见下方第五节 —— 线上搜索接口在 MySQL 下 100% 返回 500，截图只能是空态。
  该图改用本地 SQLite 实例（同一份前端产物）拍摄，能正常展示结果列表与关键词高亮。
  **界面与线上一致，数据是演示数据**，在此明确披露。

---

## 五、实测发现的线上真实缺陷（待你决定是否修复）

**`GET /api/search` 在线上恒返回 500** —— 任意关键词、带 token 或不带 token 都是
`{"code":50000,"message":"服务器内部错误"}`。同一二进制在本地 SQLite 下搜索正常（能命中 5 条），
因此可以定位到数据库方言差异。

- **根因**：`server/internal/repository/search_repo.go` 的 LIKE 子句写的是 Go 字符串
  `"LIKE ? ESCAPE '\\'"`。Go 双引号字符串已把 `\\` 还原成**单个**反斜杠，落到 SQL 文本就是 `ESCAPE '\'`。
  - SQLite 的字符串字面量不认反斜杠转义 → `'\'` 是合法的单字符常量 → 本地正常；
  - MySQL 会把 `\'` 当成被转义的单引号 → 字符串不闭合 → 语法错误 → 500。
- **建议修法（方言无关，3 处改动）**：`escapeLike` 改为转义 `!`（`!`→`!!`、`%`→`!%`、`_`→`!_`），
  SQL 里的 `ESCAPE '\\'` 改成 `ESCAPE '!'`。SQLite / MySQL / PostgreSQL 行为一致。
- 该缺陷的说明与修法已写入 README 第九节「已知限制」，**源码未改动**（本轮范围是文档）。

---

## 六、线上遗留数据（等你确认是否清理）

演示数据仍留在线上，对生产库无副作用但会一直可见：

- 知识库 **id=4「【演示】产品资料库（手册截图）」** 及其下 **16 篇文档**（id 36~51）；
- 团队 **id=2「产品研发组」**。

需要清理的话说一声，我按 id 列表逐条删除；也可以先改名为可识别的标记再观察几天。

---

## 七、验证状态

| 检查项 | 结果 |
| --- | --- |
| PDF 页数 / 体积 | 48 页 / 6.6 MB / A4 594.96×841.92pt |
| 封面 | 满页（内容占满 209.9mm），第 2 页无空白 |
| 正文列宽 | **178.5~182.4mm**（修复前仅 133.3mm，右侧留白 60mm+） |
| 目录锚点 | 17/17 全部命中 |
| 图片引用 | 52 处引用 / 49 个文件，**缺失 0、冗余 0** |
| 截图统一性 | 全部 1600×1000 |

排版根因值得记一笔：正文列宽被压到 133mm 并非 `@page` 设置问题，而是 **pandoc standalone 模板内联的
`body{max-width:36em;margin:0 auto;padding:0 50px}`**。经「裸 HTML / 含图 / 不含图 / 二分 CSS」四组对照实验定位，
在自己的 CSS 中覆盖 `max-width:none;margin:0;padding:0` 后恢复正常。

---

## 八、可复用的产出

- 新增 Skill：`~/.workbuddy/skills/screenshot-manual-pdf/SKILL.md`
  （截图采集 + pandoc/Chrome 出 A4 PDF 的完整 runbook，含列宽、tmpfs、回收站守卫、图片封顶等实测坑）
- 新增构建脚本：`tools/build/build-manual-pdf.sh`（已登记进 README 文档索引）
