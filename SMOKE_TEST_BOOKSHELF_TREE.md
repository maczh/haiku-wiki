# 多文库目录树 · 浏览器冒烟清单

> 适用环境：能联网 + 正常文件系统（非 overlayfs）的机器。本沙箱无法跑 `npm run build`，
> 故以下步骤需在你本地 / CI 正常环境执行：`npm install && npm run build && npm run dev`，
> 并同时启动后端（Go + SQLite）。
> 需要两种登录角色验证：**普通成员** 与 **管理员**。

---

## A. 前置

1. `npm install`（补齐 `docx/jquery/html2canvas/jspdf/pptxgenjs` 等导出依赖）。
2. `npm run build` 整站绿（确认无 `Cannot find module`）。
3. 起后端 + 前端 dev；分别用普通成员、管理员账号登录。

---

## B. 阅读页左侧栏：多文库目录树

- [ ] 打开任意知识库 `/books/:bookId`，左侧栏顶部显示「寄海文档」+ 当前库可见性/名称/文档数。
- [ ] 左侧栏第一级三分类齐全：**私人知识库 / 团队知识库 / 公司知识库**，分类标题显示知识库计数。
      （某类为空时也显示分类，且不报错。）
- [ ] 展开「私人知识库」→ 列出 `mine` 知识库；「团队」→ `teams`；「公司」→ `visible`。
- [ ] 展开某知识库：出现 `Spin`（懒加载 `getTree`），拉取完成后显示文档与目录；
      继续展开子目录，确认多层级嵌套正确。
- [ ] 点击左侧栏的任意知识库 → 主内容区切换到该库；点击任意文档 → 主内容区打开该文档。
- [ ] 右键文档节点：打开 / 编辑 / 新建子文档 / 重命名（就地 `Input`，回车保存 / Esc 取消）/
      复制 / 移动到其他知识库 / 导出 / 分享 / 邀请协作 / 置顶（取消置顶）/ 删除（`Modal.confirm` 二次确认，
      **局部缓存刷新**，不触发整树重载）。
- [ ] hover 知识库行出现 `···` 菜单：新建文档 / 导入 / 设置 / 删除 /（公司库 + 管理员）管理写权限。

---

## C. 新建文档（两步模态，强制先选位置）

- [ ] 在左侧栏点某知识库 `···` → 新建文档；或点顶栏书头右键 → 新建文档；或文档右键 → 新建子文档。
- [ ] **第一步**必须选 **知识库 + 目录**（目录下拉含「根目录」项）；未选齐时「下一步」按钮禁用。
- [ ] 选好位置 → **第二步**选 `doc_type`（markdown/思维导图画布/表格/接口/api…）+ 填名称 → 创建。
- [ ] 创建后该库文档树经 `reloadBookId + reloadNonce` **自动重载并展开**到新文档所在位置，
      并自动打开编辑页。

---

## D. 导入到指定目录（两步模态 + 全链路贯通）

- [ ] 在左侧栏点某知识库 `···` → 导入。
- [ ] **第一步**选 **知识库 + 目录**。
- [ ] **第二步**选方式：
      - 文件：选本地 Word / Excel / Markdown / 附件等，解析后创建文档；
      - 网页链接：填 URL，服务端代理抓取并转 markdown（`POST /api/import/url`）。
- [ ] 导入完成后回到阅读页 → 展开该知识库 → 确认新文档**落在所选目录之下**（不是根目录）。
- [ ] 验证 `parent_id` 透传：
      - 后端 `handler/import_url_handler.go` 接收 `parent_id`（默认 0）→ `CreateDocWithContent(..., parentID, ...)`；
      - 前端 `api/docs.ts` `importUrl(url, bookId, parentId)`、`ImportDialog`/`UrlImportDialog` 的 `parentId` 生效。
- [ ] 导入后树重载展开并定位到新导入文档。

---

## E. 公司知识库写权限（需管理员）

- [ ] 管理员在左侧栏对公司知识库 → `···` → `管理写权限` → `CompanyKBWritersModal`
      列出成员、授予 / 撤销写权限（管理员自身恒可写，不在表中）。
- [ ] 普通成员打开公司库应为**只读**：无新建 / 导入入口或按钮禁用；
      管理员授权后该成员重进页面出现写入口。
- [ ] 关闭弹窗后 `can_write` 即时生效，无需刷新整页。

---

## F. 书架页回归

- [ ] 访问 `/`（书架页）恢复为卡片网格：我的知识库 / 团队知识库 / 公司知识库。
- [ ] 书架页支持按名称过滤知识库、新建知识库、右键/点击编辑与删除知识库。
- [ ] 点击书架页卡片进入对应知识库阅读页，左侧栏即为多文库目录树。

---

## G. 回归

- [ ] 文档打开 / 阅读 / 编辑页主内容区功能不受影响（只改了左侧栏）。
- [ ] 顶栏「阅读 / 编辑」切换、协作、分享、导出、大纲浮层均正常。

---

## H. 已知环境缺口（仅沙箱，正常环境无此问题）

- `npm run build` 在受限沙箱不可跑（overlayfs `ENOTEMPTY` + 宿主批量删除守卫 + 缓存属 root）。
- 前端 `tsc --noEmit` 仅剩 5 个 `Cannot find module`（缺失 5 个导出依赖），可用
  `grep -vE 'node_modules/(docx|jquery|html2canvas|jspdf|pptxgenjs)'` 过滤，与本次改动无关。
