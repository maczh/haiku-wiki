# 2026-09-22

## 新建/导入流程改造（+ 菜单直达 + 类型锁定 + 文库新建子菜单）

需求：①目录/文档行尾「+」菜单选某类型时，模板画廊直接过滤出同类型模板，且跳过「选择文库与目录」步骤；②「+」菜单扩展为全部可新建类型并新增「导入文件」（同样跳过选择位置，直接导入到当前文档/目录之下）；③文库手柄「⋯」菜单的「新建」变为下级子菜单（列出全部可新建类型）但**保留**「选择位置」两步，「导入」也保留两步。

### 改动文件
- `web/src/lib/treeMenu.tsx`：`buildPlusMenuItems(parent, onCreate, onImport)` 从 5 种固定类型扩为 `DOC_TYPES` 全部 11 种（图标走 `iconForDocType`）+ 新建分组 + 导入文件。
- `web/src/components/template/TemplateGallery.tsx`：新增 `lockDocType` prop——锁定时隐藏类型筛选 chips、切分类不重置类型、模板恒按该类型过滤（空白文档入口跟随该类型）。
- `web/src/components/tree/KnowledgeTree.tsx`：新增 props `onNewDocAt` / `onImportDirect` / `onNewDocWithType`；行尾「+」走直达流程；文库「⋯」菜单「新建」改为 antd 子菜单（含全部类型+新建分组）；注意 Dropdown 不能再用 `items.map` 剥字段（会丢 `children`）。
- `web/src/pages/BookPage.tsx`：新增状态 `newDocLockedType` / `newDocSkipLocation`；`openNewDocAt`（+菜单：位置已定→画廊过滤→选模板后直达命名，folder 直达命名一步）；`openNewDocWithType`（文库子菜单：锁定类型但保留选择位置）；`openImportDirect`（+菜单直接开 ImportDialog 抽屉）；`handlePickTemplate/handlePickBlank` 按 skipLocation 分流；step2 类型锁定时用只读 Tag 代替下拉；`openNewDocFromTemplate` 也锁定类型。
- `web/src/components/tree/DocTree.tsx`（死代码）：`buildPlusMenuItems` 补第 3 参保持 tsc 归零。

### 验证
- tsc 零报错；vite build 通过；embed 刷新 + go build OK（注意 mv dist 会带走 .gitkeep，要补回）。
- 自研流程脚本 `/home/macro/.workbuddy/tmp/verify-newdoc-flow.sh`：31/31（+菜单 13 项、画廊锁定、直达命名落库 parent=所选文档、导入直达抽屉、文库子菜单 15 项、保留选择位置）。
- `e2e-folder-dir.sh` 63/63、`e2e-dashboard.sh` 40/40、`ui-doc-types.sh` 18/18。

### 套件断言随交互演进更新（e2e-folder-dir.sh，49→63 项）
之前 25 项失败全是套件过时，非产品回归：①文库菜单「新建文档/新建目录」已子菜单化（本次需求）；②目录「复制」已放开（禁用断言过时）；③概览条入口 9-21 起是 `<Button>` 不是 `<a>`，`clickLink` 已兼容；④新建文档先过模板画廊（9-21 起），套件补画廊步骤。

### 新断言陷阱（已沉淀进 build-verify skill §3.3.4）
- antd Drawer 根元素 position:fixed → `offsetParent` 恒 null，可见性断言用 `.ant-drawer-open` class。
- 进入 `/books/:id` 时当前文库节点自动展开（reloadBookId effect expand=true），expandNode 需把 already-open 视为成功。

## 补充验证（续跑）
- **无模板类型**（模板种子只覆盖 markdown/sheet/mindmap/gantt，其余 7 类画廊仅空白卡）：待办清单/需求原型经 + 菜单空白直达命名落库 parent=1 且 doc_type 正确；接口经文库子菜单空白后仍保留「选择位置」。10/10 通过（脚本 `/home/macro/.workbuddy/tmp/verify-notpl-types.sh`）。
- 生产形态自检 `embed-prod-check.sh` 17/17、`check-lazy-routes.sh` 14/14、`check-route-fallback.sh` 14/14 全绿。
- 项目 MEMORY.md 已补「新建/导入入口流程」长期约定。

## 模板功能补全（第二批需求）
需求：①选模板先小窗预览、点「使用」才建文档；②模板中心同样预览后使用；③目录树文件手柄菜单加「另存为模板」；④管理员可改/删模板；⑤修复批量目录导入模板报错。

### 改动文件
- `web/src/components/template/TemplatePreview.tsx`（新增）：Modal 预览模板正文（复用 `DocContent` 按需分发所有类型，内部 lazy 不破坏分包），footer「换一个（onCancel）/ 使用此模板（onUse）」。画廊卡片点击→开预览，点「使用」才回调 `onSelect`；模板中心页 `TemplateGalleryPage` 共用同一画廊组件自动生效。
- `web/src/components/template/TemplateGallery.tsx`：卡片 `onClick` 改为 `setPreview(t)`；`doc_templates` 列表 API 调用。
- `web/src/pages/BookPage.tsx`：新增「另存为模板」弹窗（分类 AutoComplete + 模板名 + 默认标题 + 正文预览），`openSaveAsTemplate(bookId,doc)` 调 `createTemplate`；画廊 `onSelect` 继续走原两步流程。
- `web/src/lib/treeMenu.tsx`：`buildTreeMenuItems` 文档菜单新增「另存为模板」（canWrite 时），回调 `onSaveAsTemplate`。
- `web/src/components/tree/KnowledgeTree.tsx`：新增 prop `onSaveAsTemplate(bookId,doc)`，接到 `buildTreeMenuItems` handlers。
- `web/src/pages/AdminTemplatesPage.tsx`：列表加「编辑」列 + 编辑 Modal（分类/名称/默认标题/类型/正文）；内置模板禁用编辑与删除并给说明；导入结果 `result.errors` 用 `?? []` 防御 null；「全部跳过」时给提示。
- `web/src/api/templates.ts`：新增 `createTemplate` / `updateTemplate`；`DocTemplate` 加 `created_by`。
- 后端 `server/internal/`：`model/template.go`（加 `CreatedBy`，`Builtin` 去 `default:true` 改显式写入）、`service/template_service.go`（CreateTemplate / UpdateTemplate / DeleteTemplate + SeedTemplates 显式写 `Builtin=true`）、`handler/template_handler.go`（POST /api/templates 仅本人、DELETE /api/templates/:id 仅本人、PUT /api/admin/templates/:id 仅管理员）、`router/router.go` 注册；批量导入 `failed` 初始化为 `[]`（修复 errors 为 null 致前端 `result.errors.length` 崩白屏）。

### 关键 bug 修复
- **批量导入白屏根因**：`var failed []fileErr` 零值 nil slice → JSON `null` → 前端 `.length` TypeError 白屏。改为初始化 `[]`。
- **builtin 字段全错标**：原 `Builtin bool \`gorm:"default:true"\``，GORM 对带 default 字段跳过零值，自建/批量导入的模板全被写 `builtin=1` → 管理员删不掉、不进自定义列表。改为去掉 default、SeedTemplates 显式 `Builtin=true`、CreateTemplate/导入显式 `Builtin=false`。并加**启动幂等 repair**：把 `builtin=1` 但 `created_by>0` 的脏数据修正为 `builtin=0`。

### 验证
- API 层 `verify-tpl-api.sh` 16/16（含 errors:[]、builtin 修复、自建/改/删、批量导入 1088 条归位 136 内置+1088 自定义）。
- 浏览器 `verify-tpl-ui.sh` 23/23（预览后使用、模板中心预览、另存为模板 builtin=false、管理员改名/删除、导入不白屏）。
- 回归（run-all）：e2e-folder-dir / e2e-dashboard / ui-doc-types / embed-prod-check / check-lazy-routes / check-route-fallback 共 6 套 166/166 全绿。

## 补充（需求#1 一致性收口）
排查发现**文库工作台「常用模板」**（`BookDashboard` 的 `TemplateQuickPanel`）经 `openNewDocFromTemplate` 直开「选择位置」step1、**绕过预览**，违反需求#1「选模板先预览」。其余入口已合规：全局首页常用模板经 `QuickStartModal`（已渲染 `template.content` 预览）；模板中心经画廊预览；+菜单经画廊预览。
- 修复：`BookPage.openNewDocFromTemplate` 改为 `setPreviewTpl(t)`（新增 `previewTpl` 状态 + 独立 `<TemplatePreview>`），点「使用此模板」才 `handlePickTemplate` → 选择位置；同时 `setNewDocLockedType(null)` 避免类型误锁。
- 验证：`verify-tpl-dash.sh` 6/6（点卡先弹预览、不直建、用后入选择位置）；回归 run-all 6 套 166/166 全绿。tsc 零报错、vite 构建、embed 刷新、go build OK。

## 模板观感重做（用户反馈「模板太丑、太不用心」）
用户判定 34 个内置模板观感差，要求重做「富有美感的专业模板」。确认三项决策：**① 正文用示例内容填充**（真实示例数据 + 顶部「填写说明」行，禁止 `【…】`/`____` 占位）；**② 范围 = 全部 136 个**（54 文档 + 39 脑图 + 38 表格 + 5 甘特，避免画廊新旧混搭）；**③ 同步升级全局文档排版主题**。

### 做法（先摸清三种数据格式的样式能力，再选「生成器 + polish 脚本」而非手改 136 个 JSON）
- **markdown（54）+ 新数据源**：源文件 `server/internal/repository/templates/_src/<目标json名>/<NN-名字>.md`（front matter 只写 `name`），生成器 `tools/templates/gen.py`（`--prune` 才允许删除、源未补齐则整文件跳过、正文含 `【…】` 直接报错、`--check` 只校验）。7 个目录：work-plan(5)/summary-report(9)/notice(9)/office-admin(9)/project-management(7)/software-project(8)/engineering-construction(7) = 54。统一素材池：星澜科技 / 林珂·周敏·徐一鸣·陈嘉禾·赵晓岚·吴桐·郑亦航·孙倩… / 星河数据中台·云枢 CRM·澄川供应链·岚图门户·远岚 App。
- **sheet + mindmap + gantt（82）**：`tools/templates/polish.py`（`SHEET_STYLE_VERSION` 版本闸 + `config.hkStyle` 幂等；改样式逻辑必须 `git checkout -- templates/*.json` 回滚原始数据再重跑，因表格插标题行是**行下移变换**、不可重复施加）。
  - 表格：合并标题带 + 表头浅底加粗居中 + 数据区细边框/斑马纹/金额列 `#,##0.00` 右对齐 + 合计行强调线 + `frozen` 首行冻结；画布**收紧**为 `last_r+5` / `n_cols+1`（沿用原 row=100 会留大片空白网格）。
  - 脑图：**完整主题快照**（39 键，因 `setThemeConfig` 是整份替换语义），色卡由 `tools/templates/gen-mindmap-themes.mjs` 以库默认主题 `deepMerge` 生成 4 套（ink 墨蓝 / jade 青玉 / rose 绛玫 / slate 石墨），**同源产出** `mindmap-themes.json`（模板）与 `web/src/components/editor/mindmap/mmThemePresets.generated.ts`（编辑器预设 `MM_EXTRA_THEME_PRESETS`，在 `mmShared.ts` 追加进 `MM_THEME_PRESETS`）；另重写两个空壳脑图（季度OKR目标计划、年度工作总结脑图）。
  - 甘特：占位文案修正 + 排期整体平移到基准日附近 + 按 `lib/gantt.ts taskStatus` 口径回算 progress（原先全屏「已超期」红点）。
- **全局排版主题**：`web/src/components/reader/reader.css` 顶部新增约 300 行，作用域 `.doc-content`（阅读态 + 模板预览共用，编辑器不受影响）。标题强调色竖条/圆点（`::before` 必须排在 22px 折叠箭头**之后**）、表格覆盖 Vditor 默认 `display:block` 改回 `display:table`、表头底色 + 斑马纹 + 圆角外框、`> **提示**：` 引用块成提示块、分隔线/清单/代码/mermaid 容器统一。

### 顺带修复的既有缺陷
- `MindmapView.tsx`：预览弹窗里脑图被横向裁切 —— 单次 `mm.view.fit()` 在 Modal 入场动画期间算错容器尺寸。改为「render_end fit + 320/760ms 两次延时 fit + ResizeObserver(>2px 才 rAF refit)」，清理时 disconnect/clearTimeout/cancelAnimationFrame。
- `GanttChart.tsx`：状态列 `width: 92` 容不下 5 字文案「正常进行中」被折成两行 → 加宽到 108 + `whiteSpace: nowrap`（READ_COLUMNS / EDIT_COLUMNS 两处）。

### 校验
- `gen.py --prune` 幂等复跑「无变化」；14 个 JSON 合计 136 个模板，按类型 54/39/38/5，三元组唯一无丢失；54 个源文件名称零偏差、无占位符。
- `polish.py` 后 38 个表格 `config.hkStyle==1` 全通过、39 个脑图均有完整 theme、5 个甘特状态混合（正常/已结束/未开始）。
- tsc 零报错；vite 构建通过；embed 刷新（入口 CSS `grep -c vditor` = 0，按需加载未破）；`go build` 通过。
- 无头 Chrome 生产形态实拍 before/after 对比：markdown / sheet / mindmap / gantt 四类全部改善；对比审阅页 `.workbuddy/templates-review/before-after.html`。

### 顺带修活过期套件 + 提交卫生
- `tools/verify/gantt-ui-check.sh` 第 1 段原有 2 条断言盯**旧 UI**（顶层「新建文档」菜单项、第二步可选「文档类型」下拉），自上一轮新建入口改造后一直红着无人察觉（该套件不在当时跑的 6 套名单里）。按新链路重写为 8 条：菜单含「新建」「导入」+ 旧顶层项已移除 + 「新建」子菜单含「甘特图」+ 点类型先开模板画廊（含空白文档卡）+ 进入「选择位置」+ 第二步只读 Tag 且无 Select。25 项（2 红）→ **30/30 全绿**。
- 回归 12 套 **293/293** 全绿（含 gantt 四套、embed-prod-check、check-lazy-routes、mermaid-render-check 等）。
- 提交 `7b8d005`。提交前发现根目录混入 `haiku.tar`（`docker save` 导出物 **179MB**，root 属主）与 `tools/templates/__pycache__/`，`git add -A` 会把它们卷进提交（.git 瞬间 217MB）。已 `git rm --cached` + `--amend` 剔除，`.gitignore` 补 `*.tar`/`__pycache__/`/`*.pyc`，`git reflog expire --expire=now --all && git gc --prune=now` 回收 → .git 回到 36MB。

## 编辑器四项需求（补齐/修 bug；此前 HEAD 已有同名半成品提交，留下 3 个真实缺陷）
需求：Markdown 右键菜单 H1-H6/插入/删除行 + 选区加粗等；表格文字色/背景色按钮；思维导图另存模板预览报错；新建/导入思维导图默认结构。

### 实际改动
- `web/src/lib/vditorCmds.ts`：`BlockType` 扩 h4/h5/h6；`BLOCK_PREFIX` 补 `#### `/`##### `/`###### `（`restyle`/`stripBlockMarks` 已通用处理，无需额外逻辑）。
- `web/src/components/editor/VditorEditor.tsx`：①「样式」子菜单补 H4/H5/H6 三项；②`runAction` 的块级 switch 把 h4/h5/h6 纳入 `setBlockType` 分支（**首轮工程师漏改此项 → H4-H6 点击无效，已复核补上**）。
- `web/src/components/editor/SheetEditor.tsx`：`applyCellColor` 遍历当前选区 `getluckysheet_select_save` 逐格调用 Luckysheet 公共 API `setCellFormat(row,column,attr,value)` 写色（该 API 只写单格坐标、不读选区；旧版 `(attr,value,op?,sheetIndex?)` 签名在本项目 2.1.13 **不存在**，误用会被内部行/列校验早退）。**重要更正**：HEAD 里该 `(row,col,attr,value)` 签名本就正确，本次仅把循环改写得更清晰（解构 + 注释），行为未变——颜色落色路径一直可用，并非此前所记「原错误调用」。
- `web/src/lib/mindmap.ts`：`normalizeImageSize` 的「是否有图片」判定兼容 **对象图片**（`setImage({url,...})` 把 `image` 存成对象而非字符串，原守卫 `typeof image!=='string'` 直接 return 导致 `imageSize` 永不补 → 预览 `getImgShowSize()` 解构 undefined 崩溃）。

### 关键诊断
- **默认结构实为已修复项**：`MindmapEditor` 与 `MindmapView` 均 `layout || 'mindMap'`，空白新建发空内容 → 默认思维导图；内置模板保留 `logicalStructure` 有意为之，未动。
- **Bug A 真正根因（本次坐实）**：Luckysheet 内置 `textColor`/`fillColor`/`border` 工具条下拉点击瞬间会调用 `$(".luckysheet-color-selected").spectrum(...)`（esm 内 35 处 `.spectrum(`）；项目此前未加载官方 `plugins/js/plugin.js`（内含 Spectrum），`window.$` 的 jQuery 实例缺 `$.fn.spectrum` → 抛 `$(...).spectrum is not a function`、面板永不出现。修复 = `main.tsx` 引入 `spectrum-colorpicker`（UMD 内部 `require('jquery')` 与 `window.$` 同一实例，自动挂 `$.fn.spectrum`；并兜底显式注册）。**AntD Popover 的自定义「文字颜色/单元格背景色」按钮一直能弹、能落色**（`setCellFormat(row,col,attr,value)` 签名 HEAD 即正确，非 Bug A 的一部分）。
- **验证假阴性复盘**：首轮 E2E 探针读 `/api/docs` 未带鉴权 token → 返回空 → 误判 `fc` 未落库；带 token 后 `serverFc:true` 证实落色正常。
- simple-mind-map 的 `image` 始终是**对象**（`{url,title,width,height}`），任何图片兜底都要按对象处理。

### 验证
- tsc 零报错；vite build OK；`rsync -a --delete web/dist/ server/internal/static/dist/` + 补回 `.gitkeep`；系统 Go（`/usr/local/go`，go1.25.7）重编后端嵌入二进制 `$TMPDIR/haiku-wiki`。
- **交互级 E2E（`tools/verify/e2e-editor-menus.mjs`，playwright-core + 系统 Chrome，真实输入事件）35/35 全绿**：①Markdown 右键「样式」H1-H6/引用/列表/正文 + 「插入」表格/分割线/时序图/流程图/图片/链接 + 删除行 + 表格行列增删 + 选区加粗/斜体/删除线；②**Bug B**：子菜单全部生效（根因 = antd 子菜单弹层渲染到 body 之外 + `getPopupContainer` 渲染进浮层 + `onDocDown` 放行 `.ant-menu-submenu-popup`）；③**Bug A**：内置 textColor/fillColor/border 下拉均可弹出、零控制台错误；④自定义「文字颜色/单元格背景色」按钮真实选单元格→选色块→`fc`/`bg` 落库（带 token 读 `/api/docs` 校验）；⑤零控制台错误。旧合成点击脚本 `e2e-editor-context-menu.sh` 已废弃（漏判 mousedown 路径），由 `.mjs` 取代并登记进 `run-all.sh`。
