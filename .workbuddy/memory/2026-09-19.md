# 2026-09-19 工作日志

## 甘特图文档（doc_type=gantt）功能收尾与验证

原始需求：「新增」按钮加「甘特图」文档类型，成员可增任务/子任务/改进度，阅读态只改进度，数据落后端。
经两轮增强：v2 负责人(多选+自由输入)/描述(悬停气泡)/状态灯(红超期·黄拖延·绿正常·灰已结束·蓝未开始)；
v3 新增「优先级」列(1-10) + 进度条上下半透明外框(紫罗兰色系, alpha 0.16→0.72 随优先级加深)。

### 关键技术决策与坑
- **组件选型**：`vxe-gantt` 是 Vue 3 专用，无法用于 React18 → 改用 **SVAR React Gantt**
  (`@svar-ui/react-gantt@2.7.3` + `@svar-ui/gantt-locales@2.7.2`)，MIT。
- **致命 bug（已修）**：`ganttToSvar` 不能给所有任务写 `open:true` —— SVAR `lib-state` 的 `parse()` 把每个任务
  `data` 置 `null`，`toArray()` 遇 `open===true && data===null` 抛 `Cannot read properties of null (reading 'forEach')`
  → 整图白屏。**只给有子任务的父节点写 `open:true`**（用 `hasChild` 集合判定）。
- **getTask 键类型坑（已修）**：SVAR `byId` 是 `Map`，键为**数字** id；DOM `data-id` 是**字符串**，
  悬停气泡/编辑器里拿字符串调 `getTask` 永远查不到。统一抽 `resolveSvarTask(api, id)`：先 `getTask(Number(id))`，
  失败再遍历 `serialize({data:'tasks'})` 按 `String(t.id)===String(id)` 兜底。
- **外框被 hover 覆盖（已修）**：SVAR 的 hover 规则用 CSS-Modules 哈希类，与注入选择器同特异度但加载更晚 →
  覆盖我们的 `box-shadow`。修复：选择器加深为 `.hk-gantt .wx-bar[data-id="X"]` 并对 `box-shadow` 加 `!important`。
- **零 CDN**：只能用 `@svar-ui/react-gantt/style.css`（无 url()），不能用 `all.css`（含 `@font-face` 指向 cdn.svar.dev）。
  `wxi-*` 图标字体缺失 → 本地 CSS 字符字形兜底。
- **阅读态只改进度**：`api.intercept` 拦掉 `drag-task`/`add-task`/delete/copy/move/indent/split/`*-link`，
  `update-task` 白名单仅放行 `Object.keys(ev.task).every(k=>k==='progress')`。进度手柄派 `update-task {task:{progress}}`，不走 `drag-task`（源码逆向确认）。
- **按需加载**：甘特代码在独立 chunk（`GanttChart-*.js` ≈265KB / `GanttChart-*.css` ≈35KB），入口 `index-*.js`
  中 `wx-gantt/svar/GanttChart/vxe` 计数必须为 0（已通过）。
- **状态灯/优先级前后端规则同步**：`web/src/lib/gantt.ts` 与 `server/internal/service/exportx/gantt.go`
  各实现一份，判定顺序一致（已结束→已超期→未开始→进度拖延→正常）。

### 验证结果
- 后端 API 回归 `gantt-api-check.sh`：PASS=21 FAIL=0（含 doc_type 不被静默降级的对照组）。
- 全功能端到端 `gantt-v3-check.sh`：PASS=36 FAIL=0（编辑态/阅读态、优先级列 P1/P5/P8/P10、外框 alpha 0.16/0.41/0.72、
  负责人多选+自由输入、悬停气泡含描述/负责人、落库、导出 md/xlsx 含负责人/状态/优先级/描述列、阅读态仅进度可改）。
- 产物：`web/dist` + 内嵌 `server/internal/static/dist` 已刷新，二进制 `/home/macro/.workbuddy/tmp/haiku-wiki` 已重编。

### 待办（可选/需用户批准）
- 编辑态甘特图可一并加进 `BookPage` 的 `isWideDoc`（阅读态已用 `fullWidth` 绕过）。
- 清理 `/home/macro/.workbuddy/tmp/` 下的 dist-backup 与 npm-junk（需用户批准，勿擅自删）。

## 第二轮小修（字号 + 悬停气泡）
用户反馈三处：① 表头字号偏大；② 开始日期列字号偏大；③ 除第一个任务外其他任务悬停不弹气泡。
- **悬停气泡只弹首任务（真 bug，已修）**：原用 `onMouseOver`+`onMouseOut`，跨任务移动时 mouseout/mouseover 交错触发、互相清掉对方的 `setTip`，导致只首任务稳定显示。改为 `onMouseMove`+`onMouseLeave`，
  `tipBarRef` 记住当前悬停 bar id，同一条内只跟随光标更新位置、跨条才重算任务。现在 3 个任务全部正常弹（含负责人/时间/描述）。
- **字号**：SVAR 表头/正文用 `font` 简写变量（依赖 `--wx-font-weight-md/--wx-font-size/--wx-font-family`），这些子变量未定义时整条失效回退到继承的 16px。
  改在 `gantt.css` 用 `!important` 直接覆盖：`.wx-header .wx-cell`=12px、`.wx-row .wx-cell`=12px、`.wx-cell[data-col-id=':start']`=11px。
  ⚠️ `data-col-id` 真实值是带冒号前缀的 `:start`/`:text`/...（不是 `start`），属性选择器必须写成 `[data-col-id=":start"]`。
- 验证：`gantt-fix-check.sh` 6/6、v3 36/36、api 21/21 全绿。

## 性能加固（memo 化甘特本体）
`onMouseMove` 悬停改为每帧 setTip，会让整个 `GanttChart`（含 SVAR 甘特组件）每鼠标移动都重渲染，大图表会卡。
把 `Willow+Gantt` 抽成 `memo` 组件 `GanttBody`（props = seed/mode/init，均稳定），tip 变化只重渲染外层包装层与气泡，不再重渲染甘特本体。
验证：gantt-fix 6/6、v3 36/36 仍全绿；入口 chunk 甘特关键词 0，分包未破坏。

## 编辑态悬停补充验证
甘特图阅读态/编辑态共用同一 `GanttChart`，悬停逻辑与模式无关；但实测确认编辑态下任务 A/B/C 悬停也全部弹气泡（gantt-edit-hover 3/3）。
另查 `BookPage`：`tab==='edit'` 内容区无 maxWidth 约束，编辑态甘特本就满宽；阅读态 gantt 由 DocContent 用 fullWidth 撑满。
故「把 gantt 加进 isWideDoc」并非必须，未改动以免引入回归。

## 第三轮小修（列宽固定，v5）
用户反馈：甘特图左侧表格各列宽度不允许自动调整列宽，特别是任务名称列。
- **改动（已落盘 + 编译 + 端到端验证）**：
  1. `web/src/components/gantt/GanttChart.tsx` init 内 `api.intercept('resize-column', () => false)` —— 拦截拖拽表头分隔线改列宽。
  2. 任务名称列 `id:'text'` 去 `flexgrow`，固定 `width:176`（阅读态 L110 / 编辑态 L121 两处）。
  3. `web/src/components/gantt/gantt.css` `.wx-header .wx-cell { cursor: default !important }` —— 表头禁用 resize 光标。
- **原理**：SVAR grid 宽度 = 各列 `width` 之和（源码 `react-gantt/dist/index.es.js` `tt(re)`/`o(tt(re))` 证实），去 `flexgrow` 不会新增右侧空白；`resize-column` 即拖拽改列宽 action，拦截即禁用交互。
- **验证**：`gantt-v3-check.sh` 现 **37/37**（新增列宽断言：任务名称列 `first=min=max=176px` + 表头 `cursor=default` 通过）；`gantt-api-check.sh` **21/21** 全绿（无回归）。
- **构建链路（本会话恢复执行）**：本地 typescript 类型检查 0 → `mv web/dist` 让位后 `npm run build` 0（GanttChart 独立 chunk ≈265KB，入口甘特关键词计数 0）→ 刷新内嵌 `server/internal/static/dist`（保留 .gitkeep）→ `go build` 须在 **`server/` 模块目录**（go.mod 在此，根目录报 cannot find main module）0 → 二进制 `/home/macro/.workbuddy/tmp/haiku-wiki` 已重编。
- ⚠️ 构建环境易错点（重申）：`npx tsc` 会误装无关 `tsc@2.0.4` 卡在确认提示，必须用 `./web/node_modules/.bin/tsc`；go build cwd 必须是 `server/`。

## v6（12:xx）悬停气泡只有部分任务能弹——根因与修法
- **现象**：只有最顶部第一个任务进度条悬停弹气泡，其余不弹（用户本轮反馈）。
- **根因（探针铁证）**：SVAR 时间轴里部分任务 bar 中心坐标 `elementFromPoint` 命中的真实顶层元素是 `.hk-gantt` 的**父级 flex DIV**（祖先），不是 `.wx-bar` 子节点。原悬停用 React `onMouseMove`（**冒泡监听在 wrapper**），真实顶层元素在 wrapper 祖先之上时事件只向上冒泡、**到不了 wrapper 监听器** → 该任务不弹。任务 1/2 顶层元素恰在 bar 内 `.wx-content` 上故正常。
- **修法（GanttChart.tsx）**：① 监听改 `document` **捕获阶段** `addEventListener('mousemove', onMove, true)`（从 document 向下派发，无论光标正下方是谁都触发，脱离冒泡假设）；② 命中测试改**遍历 `.wx-bar` 矩形包容**（`getBoundingClientRect` 内即命中），脱离 `e.target.closest('.wx-bar')`；③ `tipBarRef` 同条只跟光标、跨条才重算；④ `rootRef`/`wrapRef` + 卸载清理。
- **验证假阳性陷阱**：旧 `gantt-edit-hover.sh` 用 `bar.dispatchEvent(mousemove)`（target 恒为 bar）假阳性漏掉回归 → 已改为向 `elementFromPoint` 真实顶层元素派发；现 **3/3** 真绿。
- **验证**：真实悬停探针 任务A30%/B40%/C0%（任务3 此前卡 B 已修正）；强化 edit-hover 3-3；v3 37-37；api 21-21。本地已提交 `8f3eeb5`，验证报告 `gantt-hover-fix.md`。
- **通用教训（悬停类必记）**：SVAR 甘特 bar 之上常盖透明祖先容器，绝不能用 `e.target` 是否 bar 子节点判悬停；真实悬停测试必须派发到 `elementFromPoint` 顶层元素。

## v7（04:3x）左侧表格与右侧时间轴均可折叠隐藏
- **结构（探针铁证）**：SVAR 分隔容器 `.wx-layout`（`display:flex;flex-direction:row`）三直接子项 = `.wx-table-container`(左表格, `flex:0 0 <gridWidth>`)、`.wx-resizer`(4px 分隔条)、`.wx-content`(右时间轴, `flex:0 1 auto`, 内含 `.wx-area`)。此前误以为 `.wx-gantt` 是 row 容器（其实 `.wx-gantt` 是滚动 block，深度 6+ 才到面板，靠「找 .wx-resizer 的父节点」才定位到 .wx-layout）。
- **实现（零侵入）**：GanttChart.tsx 加 leftCollapsed/rightCollapsed state + 四角浮按钮（z:60）；根节点加 `hk-gantt-left/right-collapsed` class；gantt.css 隐藏该面板+分隔条、另一面板 `flex:1 1 auto;min-width:0` 填满。同一时刻至少保留一个面板可见（一侧折叠后另一侧折叠按钮隐藏）。
- **验证**：fold 18-18（左折叠时间轴 871→1316、右折叠表格 440→1315 均填满；展开恢复；悬停气泡无回归）、v3 37-37、api 21-21 全绿。本地提交 `fd153f6`，报告 `gantt-fold.md`。
- **通用教训**：SVAR 甘特折叠/分栏类改动，必须先用 DOM 探针定位真实 flex 分隔容器（不要猜 class），用「隐藏面板 + 另一面板 flex 填满」实现，比改 SVAR 内部代码稳。

## v8（12:5x）折叠隐藏后左表格丢行 —— 根因与修法
- **现象**：用户反馈「右边时间轴面板折叠隐藏后，左边表格任务内容减少了」（截图只剩 2 行，未折叠 8 行）。
- **根因（探针铁证）**：左表格**不自己虚拟化**，行来自右侧时间轴的可见区切片（`react-gantt` 内 `tasks.slice(area.start, area.end)`，`area` 由时间轴测量）。v7 用 `display:none` 藏时间轴 → 测量归零 → 只剩 2 行。实测 12 行 → 2 行；改成 `flex:0 0 0;width:0;overflow:hidden`（保持占位）则仍 12 行。
- **修法**：折叠改走 SVAR 原生 `displayMode`（`all|grid|chart`）+ `api.exec('set-display-mode',{mode})`，`init` 内 `api.on('set-display-mode')` 回读同步（SVAR resizer 自带箭头也派发同一 action）。库内宽度规则：`zt(columns,displayMode,gridWidth)` → `all`=`<gridWidth>px`、`grid`=`calc(100% - 4px)`（表格铺满、时间轴压到 0 宽）、`chart`= 0（编辑态退回 add-task 列宽 37px → 我们 CSS 用 `flex:0 0 0!important` 收掉）。
- **顺带修既有缺陷（同样只在实测暴露）**：Willow 渲染的两层 `<div class="wx-theme wx-willow-theme">` 在 SVAR 全部 CSS 里**没有任何规则** → 高度链断在这里，`.wx-gantt{height:100%;overflow-y:auto}` 的 100% 退化为 auto。实测 30 行时 `.wx-gantt` 被撑到 1092px 塞进 718px 容器（`scrollHeight==clientHeight`）→ 下侧行既看不到也滚不到。补 `.hk-gantt .wx-theme{height:100%;min-height:0}` 后 = 718/1092/auto，滚动条回到 `.wx-gantt`（折叠右侧后仍可纵向滚动）。
- **验证**：`gantt-fold-check.sh` 升到 v8 → **30/30**（右侧折叠后表格 23→23 行、时间轴 845→0 宽且 `display:flex`、表格 440→1286、折叠态 scrollTop 136/136 且首行变化、展开后逐像素还原 441/4/845、控制台 0 错误）；v3 37/37、api 21/21、edit-hover 3/3 全绿。几何探针 `gantt-layout-probe.sh`：chart 模式 = 表格 [285..285] w=0 / resizer 4 / content [289..1575] w=1286。
- **通用教训**：① SVAR 甘特左表格与右时间轴**共享可见区**，任何一侧都不能 `display:none`，必须用 `displayMode`；② `.wx-theme` 高度链必须由使用方补，缺了会静默退化为 auto（只在塞进固定高度容器后才暴露）；③ 断言要用「折叠前后相等」的相对不变量，别写死行数（虚拟化渲染窗口随滚动位置变化：滚到底时 23→20 行是正常的）。

## v8 补充：只读态（公开分享页）+ 边界验证
- `gantt-fold-edge-check.sh` **22/22**：走真实公开分享链路（`PUT /api/docs/:id/share` 建分享 → 清 `hk_token` → 匿名开 `/doc-share/:slug`，该页 `progressEditable=false` → `mode='readonly'`），只读态折叠同样不丢行（23→23），时间轴 `display=flex` 且 0 宽、表格 440→1546。
- 边界：折叠态把视口从 1600×1000 缩到 1180×760 → 时间轴仍 0 宽、表格仍铺满，渲染行 23→19（**行数是可见窗口高度的函数，不是缺陷**）；快速切换（间隔 150ms）终态正确；同一 JS 内极限连点 6 轮 → 类名/宽度/按钮三者自洽，无 React↔store 脱 sync。
- 断言口径（再次踩到）：**虚拟化列表/分组折叠类断言一律用相对不变量**，绝对值会在视口或滚动位置变化时假失败。

## v9（13:00-14:4x）功能指南（MD/PDF）+ 首页 Dashboard 改版 + 操作演示视频

三条工作流一次交付，全部跑通生产形态验证。

### ① 功能指南
- `docs/寄海文库功能指南.md`（20 章）+ `docs/寄海文库功能指南.pdf`（2.26 MB）+ `docs/.guide-style.css`。
- **PDF 工具链**：本机 pandoc 是 2.17.1.1，**没有 `--embed-resources`**（会报 unknown option）→ 用 `--self-contained`；
  再 `google-chrome --headless=new --no-pdf-header-footer --print-to-pdf`。脚本 `/home/macro/.workbuddy/tmp/build-guide-pdf.sh`。
  封面靠 `header#title-block-header::before/::after`，分页靠 `h2{page-break-before:always}`（首章除外），目录用 `columns:2`。
- **文档事实必须回查源码**：写完通读时纠了 6 处 —— ①「首个注册用户自动 admin」早已退役（`auth_service.go:61` 有注释），
  实际是种子 `admin`/`Jihai2026`；② `members` 库写权限对**所有登录用户**开放；③ 导出是**双通道**（服务端 14 种 + 浏览器端 docx/pptx/ppts/pdf）；
  ④ 导入没有「历史记录」功能；⑤ 接口文档导入按 `import_source` **整体替换**；⑥ 回收站只列**自己创建的知识库**里的文档。

### ② 首页 Dashboard
- 新页面 `DashboardPage.tsx` 取代 `BookshelfPage.tsx`（后者删除，能力抽成 `components/book/BookshelfSection.tsx`）。
  区块：欢迎条（问候+统计+两个开关）→ 8 个快捷操作 → 新手向导｜视频介绍 → 最近更新｜团队速览 → 书架。
- **新后端接口** `GET /api/recent-docs`：`recent_doc_repo.go`（可读库 id 集合 + JOIN doc_collaborators）→
  `recent_docs_service.go`（合并去重、按 updated_at 倒序、**逐条复核** `canReadBook||isDocCollaborator`、附 `can_write`）
  → handler + `jwt.GET("/recent-docs")`；3 个单测。
- **关闭与记忆**：`hk_onboard_dismissed` / `hk_intro_video_dismissed` 两个 localStorage 键；
  首次渲染即读标志位（不闪），欢迎条开关可重新打开并清键。
- **直达导入**：`BookPage.tsx` 支持 `/books/:id?import=file|url` → 打开对应导入框后**立刻清掉参数**（`setSearchParams(replace)`）。

### ③ 操作演示视频
- `web/public/onboarding/haiku-wiki-guide.mp4`（1600×900 / 167s / 18.9 MB，crf 23 preset slower）+ 设计封面 jpg。
- 流水线固化进仓库 `tools/video/`：`seed-demo.py`（公开 API 灌数据）→ `capture-shots.sh`（21 张真界面截图）→
  `build-guide-video.py`（edge-tts 合成旁白 → 每段「旁白时长+0.75s」渲染静帧+缓推镜+章节标签+逐句字幕 → concat 无损拼接）。
- **踩坑**：① 右键菜单挂在内层 span，事件向上冒泡 → 必须派发到 `.ant-tree-title span`，派发到祖先 wrapper 永不命中；
  ② 知识库树懒加载，必须**逐个展开+逐个等**（forEach 批量点会因重渲染失联）；③ `data-testid` 是 `hk-quick-${key.replace(/^on/,'').toLowerCase()}`；
  ④ 中文 drawtext 用 `/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc`（变量字体 NotoSansSC-VF 太细、字距怪）；
  ⑤ 章节时间点必须回填 `INTRO_CHAPTERS`（视频改了就同步）。

### ④ 验证矩阵（全绿）
`go test ./...` / `gofmt -l`=0 / `go vet` / `tsc --noEmit` / `verify:dashboard` 40-40 /
`e2e-dashboard.sh` 38-38（新增）/ `embed-prod-check` 17-17 / `check-lazy-routes` 14-14 /
`check-route-fallback` 14-14 / `ui-doc-types` 18-18 / `e2e-import` 10-10 / `gantt-fold-check` 30-30。
生产形态：embed 3001 文件 / 86 MB，二进制 104.6 MB（+20 MB 即视频）。入口 JS 755 KB、入口 CSS 4.0 KB，重依赖计数全 0。

### ⑤ 回归脚手架的 5 类「假失败」（产品没坏，脚本过时）
写进技能 §3.3.3 了，务必先看再改：登录是 `{account,password}`（不是 email）、注册是 `{username,name,email,password}`（没有 nickname）、
表格是 **Luckysheet**（`.luckysheet-cell-main`，单元格在 canvas 上）、首页页名 `DashboardPage`、
**ImportDialog 是 Drawer 且按需挂载**（知识库页上没有隐藏 file input，注入前要先开抽屉；`gen-upload-js.py` 里
「排除 `.ant-drawer` 内 input」的过滤恰好把要用的排掉了）。表格存储契约也已是 v3（`sheets[0].celldata[].v.v`）。

### 遗留
- `git push` 未执行（本机零 GitHub 凭据）。
- 本机无 Docker；`/home/macro/.workbuddy/tmp` 里构建备份与截图目录数 GB 未清理（需按目录逐个删）。
